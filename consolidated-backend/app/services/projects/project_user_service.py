"""
Project User Service

Business logic for project membership management.
"""

from typing import Optional, List
from sqlalchemy.ext.asyncio import AsyncSession

# TODO: Implement ProjectUserService
# - add_user(project_id, user_auth0_id, role_id)
# - remove_user(project_id, user_auth0_id)
# - get_users(project_id, page, size)
# - assign_role(project_id, user_auth0_id, role_id)
# - change_role(project_id, user_auth0_id, new_role_id)
# - get_user_role(project_id, user_auth0_id)


class ProjectUserService:
    """Project user management service."""
    pass
