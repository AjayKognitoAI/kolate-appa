"""
Patient Record Routes (MongoDB)

CRUD endpoints for patient records:
- Create, read, delete patient records
- Paginated listing
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement patient record endpoints
# POST /{project_id}/{trial_slug}
# GET /{project_id}/{trial_slug}
# GET /{project_id}/{trial_slug}/all
# GET /{project_id}/{trial_slug}/{record_id}
# DELETE /{project_id}/{trial_slug}/{record_id}
